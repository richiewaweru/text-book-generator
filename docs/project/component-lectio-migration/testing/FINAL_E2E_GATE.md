# Final E2E Gate
Must prove:
```text
Unit objective → Lesson context → General lesson spec → Intent plan → constrained candidates → component choices → canonical execution plan → exact Lectio work orders → writers/items/visuals → validation+repair → checkpoints → LessonDocument → Builder partial/final → save/reopen → render/PDF
```
Assertions: no V3 Studio orchestration; no full-catalogue selector prompt; no invalid component outside candidate set; no page pseudo-component; no valid sibling regeneration during repair; final LessonDocument valid; Builder round-trip valid; generation reaches correct terminal state.

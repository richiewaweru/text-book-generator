# Cursor Phase Execution Template
Read `00_READ_ME_FIRST.md`, `01_MASTER_CURSOR_PROMPT.md`, relevant contract/architecture docs, then the current phase file. Inspect local checkout before editing. Implement only this phase. Reuse existing code where it already owns the responsibility. Preserve dirty work. Add tests with implementation. Run regressions. Write `artifacts/reports/phase-XX-verification.md`. Do not start the next phase until GO. If local code differs from the researched paths, adapt paths while preserving the product/architecture invariants and document material deviations.

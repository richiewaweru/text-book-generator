# Release Prep Stage Pack

Use this when preparing a release or release-facing handoff.

## Expectations
- Re-run full validation.
- Update live docs and `CHANGELOG.md` as needed.
- Treat `docs/v0.1.0/` as historical snapshots unless explicitly versioning a new snapshot set.

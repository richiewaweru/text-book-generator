# Lectio 0.2.6 Downstream Rebaseline

**Date:** 2026-04-22  
**Classification:** minor  
**Subsystems:** frontend presentation/runtime integration

## Progress

- [x] Create runbook and tracking checklist
- [x] Bump frontend `lectio` dependency to `0.2.6`
- [x] Refresh npm and pnpm lockfiles
- [x] Confirm `lectio/theme.css` import remains present
- [x] Add/adjust guard test for theme import regression
- [ ] Rebaseline visual rhythm evidence (mobile/tablet/desktop)
- [x] Revalidate print output coverage for practice/short-answer/simulation/timeline/worked-example/fill-in-blank
- [x] Audit and update Lectio-surface CSS overrides for token-driven spacing/radius compatibility
- [x] Run frontend validation (`check`, `test`, `build`)
- [x] Run repo frontend validation gate (`validate_repo.py --scope frontend`)
- [x] Run optional PDF export runtime smoke check (if backend available)
- [x] Record downstream communication notes (presentation-only update, no TS/public schema migration)

## Validation Evidence

- Dependency + lockfiles:
  - `cd frontend && npm install` (pass)
  - `cd frontend && pnpm install --lockfile-only` (pass)
- Targeted test pass (new coverage):
  - `cd frontend && npm run test -- src/lib/theme-import-guard.test.ts src/lib/components/LectioDocumentView.package-smoke.test.ts`
  - Result: 2 files passed, 4 tests passed
- Frontend validation:
  - `cd frontend && npm run check` (pass, 0 errors/warnings)
  - `cd frontend && npm run test` (pass, 33 files / 114 tests)
  - `cd frontend && npm run build` (pass)
- Repo frontend validation gate:
  - `python tools/agent/validate_repo.py --scope frontend` (pass)
- Optional PDF export runtime smoke:
  - `python scripts/pdf_export_runtime_smoke.py http://127.0.0.1:8000`
  - Result: failed (backend not running locally; connection refused)

## Visual Rebaseline Evidence

- Blocked in this pass: authenticated textbook runtime route and backend session were not active in this environment.
- Mobile: pending capture
- Tablet: pending capture
- Desktop: pending capture

## Print Revalidation Evidence

- Added deterministic print-mode package smoke coverage via `PrintModeDocumentView` wrapper and unified section fixture in:
  - `frontend/src/lib/components/LectioDocumentView.package-smoke.test.ts`
  - `frontend/src/lib/components/__fixtures__/PrintModeDocumentView.svelte`
- Assertions now verify print-mode rendering markers for:
  - practice
  - short-answer
  - simulation (static print fallback caption)
  - timeline
  - worked-example
  - fill-in-blank

## CSS Override Audit Notes

- Audited and token-aligned Lectio surface wrappers:
  - `frontend/src/lib/components/LectioDocumentView.svelte`
  - `frontend/src/lib/components/PrintSectionLink.svelte`
  - `frontend/src/routes/textbook/[id]/+page.svelte`
- Replaced brittle spacing/radius literals with Lectio token-driven values using `--space-*`, `--rh-gap-*`, `--rh-pad-*`, `--rh-radius-*`, and `--rh-eyebrow-*` (with fallbacks retained).
- Intentionally app-specific values kept:
  - Semantic color choices for route-level status/error/warning surfaces and action buttons.

## Risks / Follow-up

- Manual visual rebaseline captures are still required to close the mobile/tablet/desktop evidence gap.
- Optional PDF export runtime smoke should be re-run once backend is available at `http://127.0.0.1:8000`.

## Downstream Notes

- Downstream users should update to `lectio@0.2.6` now.
- This change is presentation-level (spacing/rhythm/radius tokenization + visual baseline updates).
- No TS/public schema migration is required.

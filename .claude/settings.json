{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); exit(0 if 'frontend/package.json' in p else 1)\" && cd 'C:/Projects/Textbook agent/frontend' && pnpm install 2>/dev/null || true",
            "statusMessage": "Updating pnpm lockfile...",
            "timeout": 120
          }
        ]
      }
    ]
  }
}

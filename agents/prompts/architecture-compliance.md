Review the diff for architecture and dependency-boundary violations.

Focus on:
- imports or dependencies that break the declared architecture rules in the project context
- delivery-layer code leaking into core logic
- missing abstraction boundaries where structural coupling is being introduced

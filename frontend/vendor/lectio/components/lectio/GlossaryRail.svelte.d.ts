import type { GlossaryContent } from '../../types';
type $$ComponentProps = {
    content: GlossaryContent;
    class?: string;
    mode?: 'sticky' | 'drawer' | 'inline-strip';
};
declare const GlossaryRail: import("svelte").Component<$$ComponentProps, {}, "">;
type GlossaryRail = ReturnType<typeof GlossaryRail>;
export default GlossaryRail;

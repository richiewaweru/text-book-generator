import type { TimelineContent } from '../../types';
type $$ComponentProps = {
    content: TimelineContent;
    mode?: 'static' | 'timeline-scrubber';
};
declare const TimelineBlock: import("svelte").Component<$$ComponentProps, {}, "">;
type TimelineBlock = ReturnType<typeof TimelineBlock>;
export default TimelineBlock;

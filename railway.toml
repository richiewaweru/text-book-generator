[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
buildContext = "."

[deploy]
startCommand = "sh -c 'uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1'"
healthcheckPath = "/health/ready"
healthcheckTimeout = 15
# L01 Objective and Preset Trace (sanitized)

## Scope and tracking

Read-only trace for generation `24586f47-c8bd-415a-9a43-cc77401a299b` and Builder `6ebbcec4-fd94-494b-b163-ec4322ae2c3a`. No generation, provider call, browser mutation, database write, or source edit was performed.

Campaign classification: `INVALID_SCOPE_SELECTION` (provider-capacity generation `1 of 12`, old SHA `63390e64`). Builder linkage/UI interaction passed, but selected path lesson 0 explicitly excludes solving, so content did not match the campaign scenario. The `default` preset warning was confirmed and fixed separately. This generation is excluded from latency/pass statistics; no L02 was started.

- [x] Load project onboarding and quality instructions.
- [x] Trace Unit, approved path version, L01 lesson, and persisted provenance.
- [x] Trace generation input/context, review card, canonical plan, selection trace, and LessonDocument.
- [x] Reconcile editable Builder source linkage.
- [x] Inspect persisted structured LLM metadata and prompt availability.
- [x] Record sanitized evidence and likely root causes.
- [ ] Source correction: intentionally out of scope for this read-only trace.

## Identity and timeline

- Dedicated database: `textbook_agent_component_lectio_campaign`.
- Unit `59ad0eb4-da1e-48d3-9cbe-efe4761e4eef`, approved, subject `Mathematics`, grade `Grade 7`.
- Unit destination objective: `By the end, students can solve and check one-step linear equations.`
- Approved path version `25397571-8137-4eca-91c8-1925fe73a0fe`, version `1`, generated by `path_planner`, revision `2`.
- L01 path lesson `ce44273a-995b-4583-8d6a-4a1a67dd0b85`, position `0`, title `Identify the operation`, concept slug `equations.identify.operation`, revision `1`, `source=path_planner`.
- Generation created `2026-09-05 14:42:25.673604`, completed `2026-09-05 14:44:28.367813`, status `completed`.
- Audit trail: `POST .../path:plan` → `201` at `14:40:37.508494`; `POST .../path:approve` → `200` at `14:41:19.520692`; L01 `:prepare` → `200` at `14:42:25.988123`; generation `:review` → `200` at `14:42:26.663323`; generation `:approve` → `200` at `14:43:14.912551`; `:open-builder` → `200` at `14:44:31.275666`.

## Where the objective narrowed

The narrowing occurred in the approved path plan, before generation preparation and before Component Lectio execution. The unit contract still contains both destination capabilities:

- `unit_scope_contracts.must_establish`: solve one-step equations using inverse operations; verify solutions by substitution.
- `path_versions.source_plan_json` contains six distinct lessons. L01 is `Identify the operation`; later lessons separately cover addition, subtraction, multiplication, division, and verification by substitution.
- Persisted L01 row: objective `Identify the arithmetic operation performed on the variable in a given one-step linear equation.`; `must_establish=["classify equation as addition, subtraction, multiplication or division"]`; `exclusions=["solving the equation"]`; `primary_knowledge_type=factual`; `knowledge_type_source=path_planner`.
- This is an intentional planner decomposition, not an execution-time truncation: the path plan explicitly makes solving and checking later lessons, and L01's exclusion explicitly prohibits solving.

The generation bridge then carried that already-scoped lesson forward:

- Generation `context`: `Prepared from path lesson ce44273a-995b-4583-8d6a-4a1a67dd0b85`.
- `chunked_state_json.context.form.topic`: `Identify the operation`.
- `chunked_state_json.context.form.outcome`: the L01 identification objective; the same scope note includes `Do not introduce ...` and the exact unit terminology.
- `planning_spec_json.lesson_intent.goal` and its sole review card objective both equal the L01 identification objective.
- `lesson_provenance`: same pack/generation ID, path version, path lesson, objective hash `4456c520...f29b`, skeleton `factual.first_exposure`, `lesson_mode=first_exposure`, no deviations.

## Review card, canonical plan, and selection trace

The only persisted review card is `d8a542fb-7c17-4509-836d-30f9d23ee2d0`, titled `Identify the operation`, with the L01 objective and the two grade-level prerequisite capabilities. Canonical plan hash is `44e9a909...e0da`, revision `1`, template `guided-concept-path`.

All five canonical selections were `reason=preselected`, legal, and inherited the L01 scope:

| Role | Intent | Component | Persisted block |
| --- | --- | --- | --- |
| orient | Why it matters | `hook-hero` | `orient__hook-hero__0` |
| organise | Classify the operations | `comparison-grid` | `organise__comparison-grid__0` |
| guided | Practice with hints | `practice-stack` | `guided__practice-stack__0` |
| independent | Practice alone | `practice-stack` | `independent__practice-stack__0` |
| check | Check mastery | `quiz-check` | `check__quiz-check__0` |

The organiser grid contains an inverse-operation-to-solve reference row, but its application prompt remains classification. Guided, independent, and check purposes/questions all ask learners to identify the operation; none asks them to solve an equation or check a proposed solution.

## Persisted LessonDocument and Builder linkage

Generation `document_json` is a canonical document with five ordered sections and five blocks:

| Position | Section | Block/component |
| ---: | --- | --- |
| 0 | Why it matters | `orient__hook-hero__0` / `hook-hero` |
| 1 | Classify the operations | `organise__comparison-grid__0` / `comparison-grid` |
| 2 | Practice with hints | `guided__practice-stack__0` / `practice-stack` |
| 3 | Practice alone | `independent__practice-stack__0` / `practice-stack` |
| 4 | Check mastery | `check__quiz-check__0` / `quiz-check` |

Sanitized content-shape evidence: guided and independent each contain four operation-identification problems (addition, subtraction, multiplication, division); check contains one operation-identification quiz. The guided checkpoint is attempt `2` with `recovery=repair`; all other block checkpoints are attempt `1`. No complete document prose is retained here.

Builder reconciliation is exact: editable lesson `6ebbcec4-fd94-494b-b163-ec4322ae2c3a` has `source_type=component_lectio`, `source_generation_id=24586f47-c8bd-415a-9a43-cc77401a299b`, title `Identify the operation`, subject `Mathematics`, five sections, and five blocks with the same section/block IDs.

## Preset origin and mismatch

The persisted scalar generation metadata is internally consistent with the selected runtime path: `requested_preset_id=v3-studio`, `resolved_preset_id=v3-studio`; `requested_template_id=resolved_template_id=guided-concept-path`; `chunked_state_json.control.pipeline=component_lectio`.

The persisted LessonDocument instead has `preset_id=default`; the Builder copy has the same `preset_id=default`. This value originates in the Component Lectio assembly call:

- `backend/src/generation/component_lectio/service.py:690-697` calls `assemble_lesson_document(..., template_id=template_id, preset_id="default")`.
- `backend/src/v3_execution/runtime/lesson_document.py:21-30` also declares `preset_id: str = "default"`, but the service caller explicitly supplies the value.
- `backend/src/generation/component_lectio/launcher.py:16-47` does not pass a preset through from persisted generation metadata.
- Builder's `basePresetMap[document.preset_id]` lookup therefore receives `default`; the observed warning is the frontend's unknown-preset fallback (`frontend/src/lib/builder/components/shell/AppShell.svelte:44,153`).

Thus `default` is a document-assembly metadata propagation defect, separate from path objective selection and separate from provider output. It is not evidence that the generation scalar marker selected a `default` pipeline/preset.

## LLM metadata and prompt availability

Persisted `llm_calls` for this generation contain exactly six successful execution calls: two `v3_section_writer` calls on `standard` and four `v3_question_writer` calls on `fast`; all use `openai_compatible`, model `grok-4.3`, endpoint host `api.x.ai`. Token counts/cost fields are absent; the guided question writer's corresponding checkpoint records the one repair attempt.

No planner call is persisted for this unit: there are zero `llm_calls` rows with trace ID `unit:59ad0eb4-da1e-48d3-9cbe-efe4761e4eef%`, and zero `v3_trace_runs`/`v3_trace_events` for this generation. The `llm_calls` schema contains structured metadata only and no prompt/response columns. Consequently, the persisted `path_versions.source_plan_json` is the available planner output evidence; no raw or complete planner/provider prompt is available or retained.

## Likely root cause

1. Path planning decomposed the broad unit destination into an approved sequence and selected the operation-identification L01 as the generation target, with an explicit `solving the equation` exclusion. Component Lectio faithfully executed that scoped objective through the card, canonical plan, checkpoints, and LessonDocument.
2. Component Lectio hard-codes the legacy/default document preset during assembly, while the generation record and pipeline control marker carry `v3-studio`/`component_lectio`. Builder faithfully displays the resulting unknown `default` preset warning.

Campaign disposition: `INVALID_SCOPE_SELECTION`, not `PASS`, `PASS_WITH_RECOVERY`, or a P1 product failure. The successful Builder linkage/UI interaction remains a separate passed subcheck.
